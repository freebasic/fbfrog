#define m(a) #a

static char a[] = m(
 a b
);

static char a[] = m(
);

static char a[] = m( a);

static char a[] = m(
a);

static char a[] = m(a
b);

static char a[] = m(
a
b);

static char a[] = m(
a
b
);
