#pragma once

#define A1 docall (myfunction, (arg1, arg2))
#define A2 signed int i = (array [10 + x . p -> offset (&hFF)])
#define A3(macroparam) 10, &o10, &h10, !"a\\b", wstr(!"a\\b"), asc(!"\\"), asc(wstr(!"\\")), not l, l = r, l <> r, l + r, l - r, l * r, l / r, l mod r, l or r, l and r, l xor r, l shl r, l shr r, l andalso r, l orelse r, l or= r, l mod= r, l and= r, l shl= r, l shr= r, l xor= r, int eger, str##ing, #macroparam, #foo
